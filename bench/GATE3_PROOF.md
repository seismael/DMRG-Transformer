# Gate-3 Proof — TT-DMRG matches Dense Least-Squares (GPU)

**Device:** `device=cuda:0 (NVIDIA GeForce MX150, sm_61, 2.0 GiB)`

AGENTS.md §3 Validation Gate 3: *"The MSE of the DMRG sweep must converge to the exact same MSE as the Dense Exact Solver."*

**Setup:** N=64, M=64, TT-rank r=4, batch=512. Target `Y = X @ W_true` where `W_true` is generated to live on the rank-r TT manifold.

## Results

| Estimator | MSE | Wall time (s) |
| :-------- | --: | ------------: |
| Dense `torch.linalg.lstsq` (O(N^3), cuSOLVER) | 2.386e-30 | 0.1806 |
| TT-DMRG initial (random init) | 1.245e+01 | — |
| TT-DMRG after 1 sweep | 6.379e-02 | — |
| TT-DMRG after 20 sweeps | 1.349e-29 | 0.6316 |

**Parameter count:** TT=512 vs dense=4,096 (8.0× compression).

**MSE ratio (DMRG / dense):** 5.653e+00. Values near 1.0 (within float64 roundoff) satisfy Gate 3.

> Generated by `scripts/run_gate3_proof.py`.
